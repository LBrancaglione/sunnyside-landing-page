import styled from "styled-components";

export const Nav = styled.nav`
    height: 65px;
    border: none;
    background: none;
    padding: 0px 20px;
    display: flex;
    justify-content: space-between;

    .logo{
        padding: 15px 0;
    }
`;